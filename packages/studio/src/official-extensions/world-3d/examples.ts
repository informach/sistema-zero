import type { ExtensionExample } from '#extensions'

/**
 * Exemplos da vitrine do Mundo 3D. A v1 completa terá 4 ("Meu Mundo" flagship,
 * "Corrida do Pôr do Sol", "Boliche na Praça", "Inverno Mágico") — cada um
 * nasce de um __gen_*.ts (JS real → parse → 0 raw → IR embutida), o padrão do
 * Jogo 3D Avançado. Entram na R9, quando os blocos que eles usam existirem.
 */
export const world3DExamples: ExtensionExample[] = []
