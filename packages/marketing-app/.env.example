# URL do API Gateway (o BFF do marketing-app chama o gateway, nunca os serviços direto).
GATEWAY_URL=http://localhost:3000

# Verificação do access JWT — configure PELO MENOS UM dos dois modos:
#  (a) HS256 (dev/local): MESMO segredo do @sistemazero/auth e do gateway.
JWT_HS256_SECRET=dev-please-change-me-min-32-chars-long-secret
#  (b) RS256 via JWKS (PRODUÇÃO — o auth emite RS256; não há segredo HS256 lá).
#      Aponte para o gateway (o BFF nunca fala com o auth direto):
# JWT_JWKS_URL=http://api-gateway.railway.internal:3000/auth/.well-known/jwks.json
# ⚠️ Em PRODUÇÃO o boot EXIGE JWT_JWKS_URL e RECUSA JWT_HS256_SECRET (um segredo
#    HS256 fraco forjaria a sessão local que autoriza o gate de UI).

# Pinagem do emissor: OBRIGATÓRIOS em produção (fail-fast no boot — mesma regra
# do gateway); em dev, casar com o auth ativa a checagem (defesa em profundidade).
# JWT_ISSUER=sistemazero-auth
# JWT_AUDIENCE=sistemazero

# Não defina NODE_ENV em .env: Next/Node ajustam automaticamente para dev/build/prod.

# Sentry (opcional) — espelho de erros LOCAIS do app (exceções de Route
# Handler/RSC via onRequestError). Ausente = desligado (no-op).
# SENTRY_DSN=
