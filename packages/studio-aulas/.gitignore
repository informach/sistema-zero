# Segredos (nunca commitar — ver .env.example)
.env
.env.local

# Saídas geradas por aula (áudio, avatar, clipes de tela, vídeo final) — pesadas
aulas/**/out/

# Narração de voz por aula (mp3 pesados, gerados pelo ElevenLabs) — ficam locais, fora do git
aulas/**/audio-narracao/

# Artefatos de ferramenta
node_modules/
.remotion/
out/
test-results/
playwright-report/
*.log
