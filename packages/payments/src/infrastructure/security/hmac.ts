// Re-export de @sistemazero/core — fonte única da verdade (movido do payments).
export { signHmac, verifyHmacSignature } from '@sistemazero/core/security'
export type { HmacVerifyResult } from '@sistemazero/core/security'
