// Re-export de @sistemazero/core — fonte única da verdade (movido do payments).
export { ok, err, isOk, isErr } from '@sistemazero/core/result'
export type { Result } from '@sistemazero/core/result'
