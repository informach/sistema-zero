# dependencies
/node_modules

# next.js
/.next/
/out/
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (segredos) — .env já é ignorado no monorepo; reforço local
.env
.env*.local

# typescript (auto-gerado pelo Next)
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem

# WASM do runtime do Rive (mascote animado): DERIVADO do node_modules pelo
# `scripts/sync-rive-wasm.ts`, que roda no `dev` e no `build`. Commitar 1,8 MB
# a cada bump do pacote engordaria o histórico à toa.
/public/rive/
