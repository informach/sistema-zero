// Re-export de @sistemazero/core — fonte única da verdade (espelha payments/catalog).

export type { Logger } from '@sistemazero/core/logging'
export { createLogger } from '@sistemazero/core/logging'
